"use client";import { Sparkles } from 'lucide-react';import { LoadingSkeleton } from '@/app/[locale]/(main)/_components/map-list/loading-skeleton';import { useTranslations } from 'next-intl';export const EmptyState = ({                                     message,                                     icon: Icon,                                     isLoadingRecommendations = false,                                     hasRecommendations = false                                 }: {    message: string;    icon: React.ElementType;    isLoadingRecommendations?: boolean;    hasRecommendations?: boolean;}) => {    const t = useTranslations("MapList");    return (        <div className="flex flex-col">            {/* Empty message centered */}            <div className="flex flex-col items-center justify-center px-4 py-12 text-center">                <Icon className="mb-4 size-12 text-gray-300 dark:text-gray-600" />                <p className="mb-4 text-gray-500 dark:text-gray-400">{message}</p>                {isLoadingRecommendations && (                    <div className="mb-6 flex items-center justify-center gap-2 text-blue-600 dark:text-blue-400">                        <Sparkles className="size-5 animate-pulse" />                        <span className="text-sm font-medium">{t("searching_recommendations")}</span>                    </div>                )}                {hasRecommendations && (                    <div className="text-sm text-blue-600 dark:text-blue-400">                        <Sparkles className="mr-1 inline size-4" />                        {t("check_recommendations")}                    </div>                )}            </div>            {/* Loading skeletons full width */}            {isLoadingRecommendations && (                <div className="px-2 md:px-0">                    <LoadingSkeleton />                </div>            )}        </div>    );};