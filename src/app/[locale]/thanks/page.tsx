import { ThanksForSubscribing } from '@/app/[locale]/thanks/_components/thanks-for-subscribing';import { getTranslations } from 'next-intl/server';import { Metadata } from 'next';export async function generateMetadata({                                           params,                                       }: {    params: Promise<{ locale: string }>;}): Promise<Metadata> {    const { locale } = await params;    const t = await getTranslations('NewsletterThanks');    return {        title: t('titleMeta'),        description: t('descriptionMeta'),        alternates: {            canonical: `/${locale}/thanks`,            languages: {                'x-default': '/pl/thanks',                pl: '/pl/thanks',                en: '/en/thanks',            },        },    };}export default async function ThanksPage() {    return <ThanksForSubscribing />;}