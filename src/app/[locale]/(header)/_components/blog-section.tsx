import { BlogSectionClient } from '@/app/[locale]/(header)/_components/blog-section-client';import { getPosts } from '@/lib/posts';interface BlogSectionProps {    params: any;}export async function BlogSection({ params }: BlogSectionProps) {    const resolvedParams = await params;    const locale = resolvedParams?.locale || 'en';    const posts = await getPosts(locale);    // Get the latest post    const latestPost = posts[0] || null;    return <BlogSectionClient post={latestPost} locale={locale} />;}