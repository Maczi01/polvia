'use client';import { useEffect, useState, useRef } from 'react';import { ArrowUp } from 'lucide-react';export default function ScrollToTopButton() {    const [visible, setVisible] = useState(false);    const virtuaListRef = useRef<HTMLDivElement | null>(null); // Optional external scroll container    useEffect(() => {        const toggleVisibility = () => {            setVisible(window.scrollY > 300);        };        window.addEventListener('scroll', toggleVisibility);        return () => window.removeEventListener('scroll', toggleVisibility);    }, []);    const scrollToTop = () => {        if (virtuaListRef.current) {            virtuaListRef.current.scrollTo({ top: 0, behavior: 'smooth' });        } else {            window.scrollTo({ top: 0, behavior: 'smooth' });        }    };    if (!visible) return null;    return (        <button            onClick={scrollToTop}            className="fixed bottom-4 right-4 z-10 rounded-full bg-green-500 p-3 text-white shadow-md transition-all hover:bg-green-600 dark:bg-green-600 dark:shadow-gray-900/50 dark:hover:bg-green-500"            aria-label="Scroll to top"        >            <ArrowUp size={20} />        </button>    );}