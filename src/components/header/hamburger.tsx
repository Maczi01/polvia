import { Button } from '@/components/ui/button/button';type HamburgerProps = {    navbarOpen: boolean;    toggleNavbar: () => void;    menuId: string;}export const Hamburger = ({ navbarOpen, toggleNavbar, menuId }: HamburgerProps) => {    return (        <div className="block lg:hidden">            <Button                type="button"                className={`hamburger-icon inline-flex size-10 items-center justify-center rounded-lg bg-white dark:bg-gray-700                         p-2 text-sm text-gray-500 dark:text-gray-400 ring-2 ring-slate-300 dark:ring-gray-600                        hover:bg-gray-100 dark:hover:bg-gray-600 focus:outline-none focus:ring-2 focus:ring-gray-200 dark:focus:ring-gray-500                         transition-colors duration-200 ${navbarOpen ? 'open' : ''}`}                aria-controls={menuId}                aria-expanded={navbarOpen}                aria-label={navbarOpen ? "Zamknij menu główne" : "Otwórz menu główne"}                onClick={toggleNavbar}            >                <span className="sr-only">                    {navbarOpen ? "Zamknij menu główne" : "Otwórz menu główne"}                </span>                {/* Animated hamburger icon */}                <svg                    className="size-5 transition-transform duration-200"                    aria-hidden="true"                    xmlns="http://www.w3.org/2000/svg"                    fill="none"                    viewBox="0 0 17 14"                >                    {navbarOpen ? (                        // X icon when open                        <>                            <path                                stroke="currentColor"                                strokeLinecap="round"                                strokeLinejoin="round"                                strokeWidth="2"                                d="M1 1l15 12M16 1L1 13"                            />                        </>                    ) : (                        // Hamburger icon when closed                        <>                            <path                                stroke="currentColor"                                strokeLinecap="round"                                strokeLinejoin="round"                                strokeWidth="2"                                d="M1 1h15M1 7h15M1 13h15"                            />                        </>                    )}                </svg>            </Button>        </div>    );};